/* do not add trailing slash `/` at the end of URLs */
export const API_URL = 'http://localhost:4300';
export const scrollEventTimeout = 100;
export const debounceTimeOut = 1500;
export const ordersPerPage = 100;
export const maxElementsInDOM = 500;
export const pageQueueLimit = Math.ceil(maxElementsInDOM / ordersPerPage);
